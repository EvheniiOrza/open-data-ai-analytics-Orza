apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-app-deployment
  namespace: student-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: web-app
  template:
    metadata:
      labels:
        app: web-app
    spec:
      containers:
      - name: web-app
        image: orzayevhenii/web-app:latest
        ports:
        - containerPort: 8501
