apiVersion: v1
kind: Service
metadata:
  name: web-app-service
  namespace: student-app
spec:
  type: NodePort
  selector:
    app: web-app
  ports:
    - protocol: Tcp
      port: 80
      targetPort: 8501
      nodePort: 30080
